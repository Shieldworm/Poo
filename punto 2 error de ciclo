using System;

namespace Taller_repaso
{
    class Program
    {

        static void Main(string[] args)
        {
            int x = 1, y = 0;
            int resultado = 0;
            Console.WriteLine(1);
            while (resultado <= 10000) 
            {
              

                resultado = x + y;
                Console.WriteLine(resultado);
                y = x;
                x = resultado;
            }

        }
    }
}
