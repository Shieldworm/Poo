using System;

namespace Taller_repaso
{
    class Program
    {

        static void Main(string[] args)
        {
            int x=1, y=0;
            int resultado;
            Console.WriteLine(1);
            for (int i = 0; i <= 100; i++)
            {
              
                resultado = x + y;
                Console.WriteLine(resultado);
                y = x;
                x = resultado;
            }
            
        }
    }
}
